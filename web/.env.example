# Base URL of the API, per Vercel environment.
# Previews must point at a separate dev API: the production API allows one
# exact origin and preview deployments get random URLs.
NEXT_PUBLIC_API_URL=https://api.example.com
